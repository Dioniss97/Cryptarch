/**
 * Roles de usuario en JWT y modelo (alineado con API: guards, CRUD).
 * @type {readonly ["admin", "user"]}
 */
export const USER_ROLES = Object.freeze(["admin", "user"]);

export const ROLE_ADMIN = "admin";
export const ROLE_USER = "user";
