# sprint-00-foundation

## Goal
Create the monorepo and local infra foundation.

## In scope
- folder structure
- docker compose (web/api/postgres/redis/qdrant/worker)
- `.env.example`
- FastAPI shell + health route
- React shell
- Worker shell

## Acceptance criteria
- compose starts
- API health works
- web placeholder renders
- worker starts
- services are reachable
