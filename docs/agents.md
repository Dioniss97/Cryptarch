# Project Subagents

- `backend-architect` → FastAPI/domain/tenancy
- `frontend-admin-builder` → React admin UI
- `vector-pipeline-worker` → queue + worker + Qdrant
- `test-enforcer` → TDD discipline
- `schema-reviewer` → Postgres schema/migrations/indexes
